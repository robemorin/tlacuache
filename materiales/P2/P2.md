# Documentación del Proyecto: Generador de Exámenes (P1)

## Descripción General

Este proyecto es una aplicación web diseñada para generar exámenes personalizados para el curso **Matemáticas NM: Aplicaciones e Interpretaciones**. La aplicación permite a los usuarios seleccionar ejercicios específicos de un banco de reactivos estructurado por temas y generar una vista previa del examen, que luego puede ser impresa o guardada como PDF.

## Componentes Principales

El proyecto se compone de los siguientes archivos y directorios clave:

*   **`index.html`**: Es la interfaz de usuario principal. Carga todos los scripts necesarios y proporciona la estructura visual con un menú lateral para la selección de ejercicios y un lienzo principal para la visualización del examen.

*   **`temario.json`**: Actúa como la base de datos y el índice de todos los ejercicios disponibles. Este archivo tiene una estructura jerárquica que organiza los ejercicios por Tema, Subtema y Sección.

*   **`generar_temario.js`**: Un script de **Node.js** que construye automáticamente el archivo `temario.json`. Escanea el directorio `ejercicios/` y extrae metadatos de cada archivo de ejercicio para generar el índice. **No se ejecuta en el navegador**.

*   **`ejercicios/` (Directorio)**: Contiene los archivos JavaScript individuales para cada pregunta del banco de reactivos. El nombre de cada archivo sigue una nomenclatura de `TEMA.SUBTEMA.SECCION.EJERCICIO.js` (e.g., `1.2.1.1.js`).

*   **`src/` (Directorio)**: Contiene los archivos de lógica del frontend, como `core.js` (que maneja la carga del temario y la generación del examen en la página) y `style.css` para los estilos.

## Flujo de Trabajo

### Para Desarrolladores (Cómo agregar un ejercicio)

1.  **Crear el Archivo**: Crea un nuevo archivo `.js` en el directorio `ejercicios/` siguiendo la nomenclatura de nombrado (ej. `2.5.1.1.js`).

2.  **Importar Módulos**: Añade las siguientes líneas al inicio del archivo para importar las herramientas de `tlacuache`:
    ```javascript
    import * as tlacu from 'https://robemorin.github.io/tlacuache/src/tlacuache-modulo.mjs';
    import 'https://robemorin.github.io/tlacuache/src/tlacuache-elements.js';
    ```

3.  **Definir Metadata**: Dentro del archivo, exporta una constante `metadata` con la información del ejercicio.
    ```javascript
    export const metadata = {
        tema: "Funciones",
        subtema: "Modelos sinusoidales",
        seccion: "Aplicaciones",
        titulo: "La Rueda de la Fortuna",
        puntos: 8,
    };
    ```

4.  **Definir el Ejercicio**: Exporta una función asíncrona `generar(i)` que retorne un objeto con el `html` del problema y la `respuesta`. El parámetro `i` es el número de la pregunta y debe usarse para enumerarla.
    ```javascript
    export async function generar(i) {
        const html = `<p><strong>${i}.</strong> Enunciado del problema...</p>`;
        const respuesta = `<p>Solución detallada...</p>`;
        return { html, respuesta };
    }
    ```

5.  **Actualizar el Temario**: Abre una terminal en el directorio `P1/`, y ejecuta el siguiente comando para actualizar el índice de ejercicios:
    ```bash
    node generar_temario.js
    ```
    Esto modificará `temario.json` para incluir el nuevo ejercicio.

### Para Usuarios Finales

1.  **Abrir la Aplicación**: Abre el archivo `index.html` en un navegador web.
2.  **Seleccionar Ejercicios**: Navega por el menú de "Banco de Reactivos" en la barra lateral y marca las casillas de los ejercicios que deseas incluir.
3.  **Generar Examen**: Haz clic en el botón **"Generar Vista Previa"**. Los ejercicios seleccionados aparecerán en el área de contenido principal.
4.  **Imprimir/Guardar**: Haz clic en el botón **"🖨️ Imprimir / PDF"** para abrir el diálogo de impresión del navegador, donde puedes guardar el examen como un archivo PDF.